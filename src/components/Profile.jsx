import profileStyle from '../assets/css/profile.module.css';

const Profile=()=>{
    return(
        <div className={profileStyle.body}>
            <h1>Iam the profile...</h1>
        </div>
    );
};

export default Profile;